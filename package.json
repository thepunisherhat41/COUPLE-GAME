{
  "name": "couple-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Party game para casais, amigos e solteiros.",
  "scripts": {
    "test": "node --check app.js && node scripts/validate-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
